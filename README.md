# songbase
A platform for users to curate their own personalized music streaming platform

**Before running:**
The backend uses `ffmpeg` for audio processing MP3->PCM WAV. Ensure you have `ffmpeg` installed prior to running the program.

